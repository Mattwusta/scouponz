
<br><br>
<center>
  <font color="gray" size ="6" face = "Baskerville Old Face">人一輩子能吃飯的次數是有限的</font>    
</center>

<br><br><br>

<div class="hero-unit">

<div class="container-fluid">
  <div class="row-fluid">
   <div class="span2">
     <%= image_tag @user.image %>
    </div>
 
 
    <div class="span10">
   <font color="gray" size="5"><b> <%= @user.name %></b></font><br>
 <% if @user.intro.present? %>
   <font color="gray" size="3"> <%= @user.intro %></font><br>
   <% else %>
   <font color="#5CA5FF" size="4" color><i> “您可以在右上角選單個人 > 編輯資料裡填入自我介紹，地址，電話等資訊。”</i></font>
   <% end %>
   
   <% if @user.address.present? %>
   <font color="gray" size="2"><b>地址:</b> <%= @user.address %></font><br>
   <% else %>
   <% end %>
   
   <% if @user.phone.present? %>
   <font color="gray" size="2"><b>電話: </b> <%= @user.phone %></font>
   <% else %>
   <% end %>

   <br>
    </div>
  </div>
</div>
<hr>


<font color="gray" face = "Baskerville Old Face"> <h3>編輯個人資料</h3></font>
<br>
<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :image, label: "更換大頭照" %>
    <%= f.input :name, :required => true, :autofocus => true %>
    <%= f.input :email, :required => true, :autofocus => true %>
    <%= f.input :phone, label: "聯絡電話" %>
    <%= f.input :address, label: "送餐地址" %>
    <%= f.input :intro, label: "自我介紹", as: :text, input_html: { :class => "span6", rows: "2", :placeholder => "我是火星人，剛移民來地球，想去台東玩，因為朋友要請我情啾看！"} %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
    <% end %>

    <%= f.input :password, :autocomplete => "off", :hint => "若不更改請留空白", :required => false %>
    <%= f.input :password_confirmation, :required => false %>
    <%= f.input :current_password, :hint => "需要你目前的密碼來完成確認", :required => true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "更新" %>
  </div>
<% end %>

<h4>取消帳號</h4>

<font size="2"><p>不開心嗎? 可否寫EMAIL罵客服就好，但不要走好嗎？ <%= link_to "取消帳號", registration_path(resource_name), :data => { :confirm => "確定嗎?" }, :method => :delete %></p>

<%= link_to "上一頁", :back %></font>
</div>


