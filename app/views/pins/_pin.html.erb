
<div class="box">
  <%= link_to (image_tag pin.image(:medium)), pin %>
<br><br>


<p class="主題">
    <b><font color="#5CA5FF" size="2"><%= truncate(pin.name, :length => 30) %></h6> </font></b>
</p>

<p class="內容">
 <font size="2"> <%= truncate(pin.description, :length => 40)  %></font>
</p>

<p>
    <strong>
        <b>- 看更多-> </b> <%= link_to pin.user.name, pin.user %>
    </strong>
</p>

<% if current_user == pin.user %>

<%= link_to '編輯', edit_pin_path(pin) %>
<%= link_to '刪除', pin, method: :delete, data: { confirm: '你確定嗎?' } %>

   <% if pin.yt_video_id.present? %>
   <i class="icon-facetime-video"></i> 
   <% else %>
   <% end %>

  <% if pin.instaid.present? %>
 <i class="icon-camera"></i> 
   <% else %>
   <% end %>

    <% if pin.instapid.present? %>
 <i class="icon-camera"></i> 
   <% else %>
   <% end %>



<% else %>


<% if pin.yt_video_id.present? %>
   <i class="icon-facetime-video"></i> 
   <% else %>
   <% end %>
<% end %>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-44036574-1', 'chef29.com');
  ga('send', 'pageview');

</script>
</div>
